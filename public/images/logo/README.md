Đặt 2 file logo vào đây, đúng tên:

- `logo-black.svg` — logo màu đen, dùng trên nền sáng
- `logo-white.svg` — logo màu trắng, dùng trên nền đen (header, footer, menu overlay)

Ưu tiên SVG (nét, nhẹ). Nếu chỉ có PNG thì đặt cùng tên với đuôi `.png`, nền trong suốt,
cạnh dài ≥ 800px.
