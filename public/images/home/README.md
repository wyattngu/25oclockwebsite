Ảnh hero trang chủ (banner đầu trang, full-bleed):

- `hero-desktop.jpg` — ảnh ngang, cạnh dài ≥ 2400px
- `hero-mobile.jpg` — bản dọc của cùng ảnh, dùng trên điện thoại

Có thể dùng `.webp` thay `.jpg` nếu có sẵn.
